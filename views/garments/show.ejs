<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Garment | <%= garment.name %></title>
</head>
<body>
  <h1><%= garment.name %></h1>
  <ul>
    <li>Location: <%= garment.location %></li>
    <li>Contact: <%= garment.contact %></li>
  </ul>

  <h2>Products</h2>
  <ul>
    <% for(let product of products) { %>
      <a href="/products/<%= product._id%>">
        <li><%= product.name %> - <%= product.brand %></li>
      </a>
    <% } %>
  </ul>
  <a href="/garments">All Garments</a>
  <a href="/garments/<%= garment._id %>/edit">Edit</a>
  <form action="/garments/<%= garment._id %>?_method=DELETE" method="post">
    <button ytpe="submit">Delete</button>
  </form>
  <a href="/garments/<%= garment._id %>/product/create">Add Product</a>  
</body>
</html>